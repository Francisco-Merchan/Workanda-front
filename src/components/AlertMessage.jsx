import React from "react";

const AlertMessage = () => {
  return (
    <span className="alert-message animate__animated animate__fadeIn">
      No existen usuarios
    </span>
  );
};

export default AlertMessage;
