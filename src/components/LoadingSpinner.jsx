import React from "react";
import { ClipLoader } from "react-spinners";

const LoadingSpinner = () => {
  return (
    <section className="layout-auth-section">
      <ClipLoader color="#de4137" />
    </section>
  );
};

export default LoadingSpinner;
